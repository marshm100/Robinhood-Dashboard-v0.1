# Database (Vercel injects POSTGRES_URL automatically; fallback to SQLite)
DATABASE_URL=sqlite:////tmp/portfolio.db

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=*

# Vercel Blob token (optional, for archiving uploaded CSVs)
BLOB_READ_WRITE_TOKEN=
