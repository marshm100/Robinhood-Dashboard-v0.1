{
  "name": "robinhood-dashboard",
  "version": "1.0.0",
  "description": "Dashboard to visualize Robinhood trading history",
  "scripts": {
    "start": "node start.js",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
  }
} 